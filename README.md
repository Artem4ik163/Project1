# 💳 Bank API — In-Memory (Этап 0)

Простое REST API для работы с банковским счётом.  
**Данные хранятся в оперативной памяти** — не требует подключения к базе данных.

> 💡 Этот проект — минимальная рабочая версия (MVP), созданная по принципу:  
> **«Сначала простой функционал, потом — усложнение»**.

---

## 📌 Реализованные операции

| Метод | Endpoint | Параметры | Описание |
|-------|----------|----------|--------|
| `GET` | `/api/bank/getBalance` | `userId` | Получить текущий баланс |
| `POST` | `/api/bank/putMoney` | `userId`, `amount` | Пополнить счёт |
| `POST` | `/api/bank/takeMoney` | `userId`, `amount` | Снять деньги |
| `POST` | `/api/bank/transferMoney` | `currentUserId`, `targetUserId`, `amount` | Перевести деньги другому пользователю |
| `GET` | `/api/bank/getOperationList` | `userId`, `from` (опц.), `to` (опц.) | Получить список операций за период |

> 📅 Формат даты: `yyyy-MM-ddTHH:mm:ss` (например: `2025-12-31T15:30:00`)

---

## 📤 Формат ответа

Все ответы возвращаются в формате JSON:

```json
{
  "status": 1,
  "message": "Успех"
}





